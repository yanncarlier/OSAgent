[project]
name = "OSAgent"
version = "0.1.0"
description = "Safety-first Ubuntu 24.04 sysadmin agent with local or remote OpenAI-compatible inference"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.33.0",
]
